# Send SMS Prototype

## Run App
### Backend

In the terminal run the following commands:

```bash
cd backend
node sendSms
```

### Frontend

In the terminal run the following commands:

```bash
cd frontend
npx serve
```